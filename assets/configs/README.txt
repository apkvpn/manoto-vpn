Replace default_servers.json with your supplied VLESS/Trojan entries before release.
The app UI never renders the URI itself; it only renders a server name, protocol and ping.
Do not publish raw credentials/URIs in screenshots, documentation, or source-control history.
